server:
  port: 8082

spring:
  application:
    name: player-session
  data:
    redis:
      host: localhost
      port: 6379

---
spring:
  config:
    activate:
      on-profile: docker
  data:
    redis:
      host: redis

